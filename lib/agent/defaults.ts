/**
 * Default number of **content** slides for weekly/presentation decks (BOA-001).
 * The builder adds one opening title slide, so total pages = this + 1.
 */
/** Výchozí počet obsahových slidů (bez titulku), pokud uživatel neuvede jinak — větší deck rozloží body. */
export const WEEKLY_REPORT_DEFAULT_SLIDE_COUNT = 4;
